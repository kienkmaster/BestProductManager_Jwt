/**
 * Cấu hình endpoint API (API Endpoints).
 * Mục tiêu: quản lý tập trung các URL gọi API.
 */
export const API_ENDPOINTS = {
  account: {
    login: '/api/Account/login',
    register: '/api/Account/register',
    me: '/api/Account/me',
  },
};
