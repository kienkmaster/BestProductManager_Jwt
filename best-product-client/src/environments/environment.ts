export const environment = {
  /**
   * Cờ xác định môi trường production (prod).
   * false nghĩa là môi trường phát triển (dev).
   */
  production: false,

  /**
   * Base URL (đường dẫn gốc) của API để client gọi HttpClient (HTTP).
   */
  apiBaseUrl: 'http://localhost/BestProductManager/api',
};
